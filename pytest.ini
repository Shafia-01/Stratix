[pytest]
testpaths = tests
addopts = --import-mode=importlib
markers =
    unit: marks tests as unit tests (fast, no integration/external calls)
    integration: marks tests as integration tests (slower, using local db or complete flows with mocks)
